
const canvas = document.getElementById("mycanvas");
const ctx = canvas.getContext("2d");

ctx.moveTo(250, 50)
class Planet{
    constructor(x, y, radius, vel, orbitRadius){
        this.x = x;
        this.y = y;
        this.radius = radius;
        this.vel = vel;
        this.orbitRadius = orbitRadius;
    }

    draw(){
     
        

    }
}